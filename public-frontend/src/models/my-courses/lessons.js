// @flow

type LessonType = {
    id: number,
    name: string,
    isViewed: boolean,
    viewedAt: Date | null
};

export {
  // eslint-disable-next-line import/prefer-default-export
  LessonType,
};
