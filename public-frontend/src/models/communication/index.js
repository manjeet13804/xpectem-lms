// @flow
type CommunicationType = {
  id: number,
  title: string
};

type CommunicationDialogIdType = {
  currentCommunicationCourseId: number | string,
  currentCommunicationId: number | string
};

export default {
  CommunicationType,
  CommunicationDialogIdType,
};
