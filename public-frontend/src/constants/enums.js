const ENVIRONMENT_ENUM = {
  production: 'production',
  development: 'development',
};

const USER_ROLES_ENUM = {
  xpectum: 'xpectum',
  designer: 'designer',
  student: 'user',
  superAdmin: 'super_admin',
  admin: 'admin',
  tutor: 'tutor',
  editor: 'editor',
  none: 'none',
};

const POPUPS_ENUM = {
  notice: 'notice',
  profile: 'profile',
};

const COURSE_TABS_ENUM = {
  topics: 'topics',
  studyPlan: 'study-plan',
  communication: 'communication',
  information: 'information',
  certification: 'certification',
  faq: 'faq',
};

const STUDENTS_DATA_PARAMS_ID_ENUM = {
  emails: 'emails',
};

export {
  ENVIRONMENT_ENUM,
  USER_ROLES_ENUM,
  POPUPS_ENUM,
  COURSE_TABS_ENUM,
  STUDENTS_DATA_PARAMS_ID_ENUM
};
